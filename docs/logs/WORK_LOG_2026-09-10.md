# 工作日志 — 项目审查（2026-09-10）

## 任务
拉取并通读 GitHub 项目 `wyqyj/Dusk-Rain-Note-MuYvJian-`（暮雨笺 v3.0.8），随后进行安全与缺陷审查。

## 完成的工作

### 1. 拉取与环境确认
- 将仓库克隆至 `D:\工作区\暮雨沉山`，检出至 `main`（最新提交 `53be0df release: v3.0.8`）。
- 核对技术栈：Electron 33 + React 18 + TypeScript + Vite + CodeMirror 6 + Zustand + Tailwind；单测框架 vitest（4 个测试文件）。

### 2. 结构与文档通读
- `README.md`、`MAINTENANCE_GUIDE.md`、`CHANGELOG.md`、`UPDATE_NOTICES.md`：明确功能边界（便签 / 待办 / 速记 / 无限画布 / 考研工作台 / AI 助手）与发布、回归流程。
- `src/shared/types.ts`：确认共享领域模型（`Note`、`Workspace`、题册、画布）。

### 3. 主进程逐行审查
- `src/main/index.ts`（930 行）：窗口生命周期、IPC 表面（约 40 个 handler）、AI 流式请求、导入导出、Pandoc 编译。
- `src/main/workspaceStorage.ts`（248 行）：数据目录 bootstrap、迁移（SHA-256 校验）、gzip 备份与恢复、题册 / 书籍托管。
- `src/main/preload.ts`：contextBridge 暴露面与主进程 handler 的一致性。

### 4. 渲染层关键路径审查
- `utils/markdown.ts`：Markdown + KaTeX 渲染管线、LaTeX 结构预处理、Wiki 链接、任务复选框。
- `utils/sanitizeHtml.ts`：DOMPurify 白名单 + 二次 DOM 遍历消毒。
- `store/noteStore.ts`：内存状态、500ms 防抖整文件写盘、20 份版本快照。
- `utils/useAutoSave.ts`、`utils/planTasks.ts`：自动保存计时、计划导入解析。

### 5. 审查结论
共整理出 **15 项问题**，按严重程度分为高（3 项）/ 中（5 项）/ 低（7 项）两档之外的细分如下：

| 级别 | 数量 | 代表问题 |
| --- | --- | --- |
| 高 | 3 | notes.json 多窗口写竞争丢数据；`update-quick-note` 原型链污染；备份恢复无 gzip 解压上限 |
| 中 | 5 | 透明度钳制不对称；KaTeX 回退分支未转义；`$` 误吞货币文本；跨全文正则；生产回退加载 localhost |
| 低 | 7 | 导出备份携带整机配置；dataURI 存储放大并发风险；字体硬编码；AI 流推送到已销毁窗口；路径校验 realpath 不一致；生产菜单暴露 DevTools；测试覆盖不足 |

详见修复计划文档：`../archive/FIX_PLAN.md`。

## 未做的事
- 未运行 `npm ci` / `npm test`（本次为静态审查，未启动应用）。
- 未通读大体积组件（`StudyWorkbench.tsx`、`CanvasBoard.tsx`、`Editor.tsx`）的全部交互细节，已列入后续轮次。

## 下一步
按 `../archive/FIX_PLAN.md` 分班次实施修复，优先处理高优先级的 #1（写竞争）与 #2（原型链污染）。

## 修复实施进度（同日第二次更新，本轮代码修复已全部完成）

### 已完成（15 项审查问题中 13 项已修复，#10 与 #15 按计划进入后续迭代）
- **#1（写竞争，含渲染侧收尾）**：新增 `src/main/notesData.ts`（文件级串行写队列 `enqueueFile`、按 id/`updatedAt` 合并的 `mergeNotes`、`writeAtomic`），配单测；`save-notes` 改为读盘合并后原子落盘，`create-quick-note`、`update-quick-note-content`、`update-quick-note`、`save-attachments`、`save-timer-record`、`save-active-session` 全部接入写队列。渲染侧：preload `saveNotes` 增加 `knownAfter` 参数、`vite-env.d.ts` 类型同步、`noteStore` 记录 `lastLoadedAt`（含 reload 监听路径）随保存传入。
- **#2（原型链污染）**：`update-quick-note` 的更新载荷先经 `sanitizeNoteUpdates` 过滤 `__proto__`/`constructor`/`prototype` 再合并。
- **#3（备份 zip 炸弹）**：`restoreBackup` 增加条目数（10 万）与解压总量（512MB）上限，全部校验通过后才落盘。
- **#4（透明度钳制）**：`clampOpacity` 统一写入、读取、窗口创建三处钳制到 [0.2, 1]。
- **#5（KaTeX 回退转义）**：新增 `escapeHtml`，markdown.ts 全部 8 处渲染失败回退分支统一转义。
- **#6（货币 $ 误判）**：采用 pandoc 规则——开 `$` 后非空白、闭 `$` 前非空白且后非数字；`renderInlineMath` 与 markdown-it `math_inline` 规则同步修正。
- **#7（跨全文正则）**：`normalizeMathMarkdown` 中 `\[...\]` 禁止跨空行且限长 10000 字符，`\(...\)` 限单行 2000 字符。
- **#8（生产回退 localhost）**：抽 `loadRenderer`，打包后渲染产物缺失时弹错误框；CSP 的 localhost 例外只在开发注入。
- **#9（导出携带加密 Key）**：`export-data` 剔除 `encryptedAiApiKey`，`import-data` 忽略该键。
- **#11（PDF 字体）**：抽 `runPandoc`，按 Microsoft YaHei → Noto Sans CJK SC → SimSun → PingFang SC 回退，全失败时报出已试字体。
- **#12（AI 流推送已销毁窗口）**：`requestAi` 循环检查 `sender.isDestroyed()` 并中止，发送统一经存在性守卫。
- **#13（题册路径校验）**：`readQuestionBook` 改用 `realpathSync` 前缀比较，与 `isManagedBookPath` 一致。
- **#14（生产菜单）**：打包后隐藏「重新加载/强制重新加载/开发者工具」。

### 验证结果
- `npm ci` 完成（electron/esbuild 的安装脚本被 allow-scripts 策略拦截，不影响本次测试与类型检查）。
- `npm test`：6 个测试文件、22 个用例全绿（新增 `notesData.test.ts` 与 `markdown.test.ts`）。
- `vite.config.ts` 增加 vitest include，把位于 vite root 之外的 `src/main` 测试纳入。
- `tsconfig.main.json` 排除 `*.test.ts`，避免测试打进产物。
- `npm run typecheck`：渲染与主进程双侧均通过（修复了缺 `dialog` 导入与一处类型断言）。

### 顺延到后续迭代（#10 已于本轮完成，见下方新增章节；#15 主体已覆盖）
- **#15（测试覆盖）**：已补齐 #1–#3、#5–#7、#10 的单测；`workspaceStorage` 备份校验逻辑因强耦合 electron，单测留待抽离纯函数后补齐。

### 未执行
- `npm run build` / 打包验证（esbuild 安装脚本被本机 npm allow-scripts 策略拦截，需先 `npm approve-scripts` 后另行验证）。
- 需要真实窗口环境的手工回归（速记与主窗口交叉操作、初始化二次确认等），见 FIX_PLAN「发布前检查」。

## #10 图片磁盘化完成（同日第三次更新）

### 实现方案：attachment: 令牌
- 新增 `src/renderer/utils/attachmentRef.ts`：图片二进制写入工作台 `attachments/` 目录，笔记/画布内容中仅保存 `attachment:<文件名>` 令牌；渲染时（Markdown 管线末端 `resolveAttachmentTokens`）解析为 `file://` 绝对路径，工作台目录迁移后内容无需改动。
- 主进程新增 `attachment-write-file` IPC（含 20MB 上限与文件名消毒），`attachmentTokenToUrl` 剔除文件名中的路径分隔符防止越界。
- `attachmentStore` 重写为异步 `addAttachment(file)`：优先落盘存 `fileName`，浏览器环境回退 dataURI；`loadAttachments` 后对老数据中的 dataURI 素材做惰性迁移；新增 `attachmentDisplayUrl` 展示辅助。
- 接入点：`Editor` 插图走 `saveImageToAttachments`；画布插图/拖拽/粘贴/素材库选取统一存令牌；`sanitizeHtml` 放行 `attachment:` src；导出 Word/PDF/pandoc 编译时主进程经 `resolveAttachmentTokens` 还原为真实路径。
- 渲染修正：令牌解析对 POSIX 绝对根路径的 `file://` 前缀单独处理（避免四斜杠）；素材库选取路径同样存令牌而非已解析的 file:// URL。

### 验证结果
- 新增 `attachmentRef.test.ts`（6 用例：令牌识别、根路径解析、路径穿越剔除、HTML 替换、透传）。
- `npm test`：7 个测试文件、28 个用例全绿。
- `npm run typecheck`：主/渲染双侧通过。

### 当前状态小结
- FIX_PLAN 中 #1–#14 全部完成代码修复并通过测试；#15 主体完成（28 用例）。
- 按用户要求本轮不打包：`npm run build`/`electron-builder` 与真实窗口手工回归未执行。

## 2026-09-10 第三阶段（Agent Bridge 实现 + 文档整理）

### 对外接入 Agent（已实现）
- 新增 `src/main/agentBridge.ts`：HTTP 桥服务（Bearer 令牌、512KB 体上限、10s/30 次限流、200 条审计环形日志），能力白名单 `workspace.status / notes.list·get·create·update / plan.list·addTask·completeTask / questionBook.list·import`，写路径全部走 `enqueueFile`。
- 主进程 IPC：`agent-bridge-get/save/reset-token/token`；启动时按配置自动拉起；`before-quit` 停止服务；导出备份剔除 `agentBridge`（含 token）；导入数据忽略该键。
- `preload.ts` 与 `vite-env.d.ts` 双侧同步新方法 `getAgentBridge / saveAgentBridge / resetAgentBridgeToken`。
- 设置页新增 `AgentBridgePanel.tsx`：开关、仅本机/局域网（局域网有二次确认）、令牌复制与重置、最近 10 条调用审计。
- MCP 适配器 `bridge/muyujian-mcp.js`（零依赖 stdio MCP server）+ `bridge/README.md`（HTTP 调用示例、Codex `config.toml` 示例、能力表）。
- 新增 `agentBridge.test.ts` 8 用例；修复过程中统一了能力返回形状（`notes.create/plan.addTask` 返回 `id`，`questionBook.import` 返回题册文件夹名，参数兼容 `markdown/content` 与扁平/嵌套更新）。
- 验证：`npm test` 36 用例全绿；`npm run typecheck` 双侧通过。

### 文档
- 新增 `../archive/INLINE_AGENT_PLAN.md`：内置 Agent 功能计划书（分层架构、共享能力层抽取、function calling 循环、确认闸门、里程碑 A-D 约 3 工作日）。
- 新增 `../HANDOVER.md`：工作交接文件（项目速览、已完成工作、关键文件索引、本机环境注意事项、接手执行顺序、遗留事项）。

### 说明
- 按用户要求本轮仍不打包；dev 环境已重启（vite 5173 + electron 前台会话），设置页可见新面板。


## 2026-09-10 第四阶段（内置 Agent：DeepSeek Harness，阶段 A–E 完成）

### A 能力层抽取
- 新增 `src/main/capabilities.ts`：`CapabilitySpec`（name/description/inputSchema/sideEffect/handler）+ `createCapabilityRegistry(workspaceRoot)`；`agentBridge.ts` 改为消费注册表，行为不变。

### B 运行时拉通
- 新增 `src/main/dshRuntime.ts`：经 `@deepseek-ai/dsh-sdk-client`（stdio JSON-RPC）拉起 `node dsh --profile sdk`；惰性启动、崩溃/TransportClosedError 后重建、「停止」= 重启运行时（SDK 无轮次中取消）；API Key 经 `DEEPSEEK_API_KEY` env 注入子进程，不落盘。
- 依赖钉死 `@deepseek-ai/dsh@0.1.5-rc.1`、`@deepseek-ai/dsh-sdk-client@0.0.1-rc.1`；子进程 env 注入 `NO_PROXY=127.0.0.1,localhost` 防本机代理拦截。
- IPC：`dsh-agent-run/stop/status`；AiChat 加「问答/Agent」模式切换，Agent 模式存 `dshSessionId` 跨轮续会话。

### C 业务工具插件
- `src/main/agentToolManifest.ts`：把能力注册表序列化为 `<DSH_HOME>/muyujian-tools.json`。
- `plugins/muyujian-dsh-tools/`（Cordis 插件）：读清单 + env 注入的 Bridge 地址/token，用 `defineTool` 注册工具（能力名点号转下划线），execute 回调主进程 Bridge。
- `prepareSdkProfile()`：首启用 `--dump-config` 物化 sdk profile；同步插件进 profile node_modules；生成 `muyujian.cordis.yml` patch（挂载插件 + 禁用 tool-bash/pwsh/jobs/fs/fs-search/web/subagent/subagent-fork/workflow）。反向冒烟验证：插件损坏时 dsh 启动即报错，确认加载链路真实生效。

### D 确认闸门
- `agentBridge.ts` 新增 `confirmWrite` 钩子：写操作挂起 HTTP 响应等待确认，拒绝/异常返回 403，审计记录 approved/denied。
- `index.ts` 内部 Bridge 独立实例（loopback、每次启动随机 token、不落盘）；渲染端事件 `dsh-agent-confirm-request` + 回执 `dsh-agent-confirm-resolve`，120s 超时自动拒绝。
- AiChat 渲染确认卡片（能力描述 + 参数 + 同意/拒绝）；CSS `ai-confirm-*` 含暗色适配。

### E 依赖内置
- `scripts/vendor-dsh.mjs`：导出钉版 dsh 生产依赖到 `resources/dsh-runtime/`（实测 **212.7MB**，超出计划预估 80–150MB，裁剪待做）。
- `scripts/fetch-node.mjs`：下载内置 Node 22.14.0 到 `resources/node/`（79.5MB）。
- `resolveDshRuntimePaths()`：打包走 resources，dev 优先 vendor 目录、回退主仓库 node_modules；package.json 配好 extraResources（plugins/dsh-runtime/node）；`resources/dsh-runtime`、`resources/node` 已 gitignore。

### 验证
- `npm test`：45 用例全绿（新增 dshRuntime 6 例、bridge 闸门 3 例）；`dshProfile.integration.test.ts`（`DSH_INTEGRATION=1` 才跑）通过。
- `npm run typecheck`、`npm run build:main` 通过。

### 遗留
- electron-builder 打包验证仍按既定决定不做。

### 自定义网关接入 + 真实 E2E（用户指定 kimi-k3）
- `DshRuntime` 新增 `getModelConfig` 依赖：index.ts 传入 AI 配置 getter，运行时生成 `$DSH_HOME/settings.yaml`（`llm-pi-ai.providers.muyujian-gateway`，`api: openai-completions`、`apiKeyEnv: MUYUJIAN_LLM_API_KEY`，Key 只走环境变量不落盘）；保存 AI 配置时 `restart()` 重建 harness。
- 配置：网关 `https://api.yujianwudi.top`、模型 `kimi-k3`（`/v1/models` 探测通过）。
- `dshE2E.live.test.ts`（`DSH_LIVE_KEY` 门控）真实联通：中文路径工作区（「工作区 导入验证」）+ kimi-k3 实际决策调用 notes_create 建笔记成功。

### 路径裁剪 bug 修复
- 现象：dsh 导入工作区时中文路径第三个字被裁掉（短路径/8.3 名导致），路径不存在。
- 修法：`dshRuntime.ts` 新增 `toLongRealPath()`——win32 下 `fs.realpathSync.native` 把 8.3 短名/链接归一为长路径；不存在则原样返回。launch cwd 统一走它；`workspace-migrate` 后重启 dsh 与内部 Bridge。
- 单测 2 例（中文目录归一化 + 不存在路径容错）；E2E 以「中文+空格」工作区作回归。
- 体积裁剪：用户明确不需要，dsh-runtime 212.7MB 全量内置。

### 阶段 A–E 最终验证
- `npm run typecheck`、`npm test`（47 通过 / 2 跳过）、`npm run build:main` 全绿。
- `DSH_LIVE_KEY=... npx vitest run src/main/dshE2E.live.test.ts` 通过（30s，真实模型调用）。
- 计划书 F 阶段（文档与交接）收尾后全阶段完成。

### 启动实测抓到两个严重问题（已修 1 个，第 2 个正在做）
1. **ESM 崩溃**：`@deepseek-ai/dsh-sdk-client` 是纯 ESM，主进程 CJS 顶层 require 让 Electron 启动即抛 `ERR_REQUIRE_ESM`（单测在 Vitest ESM 环境跑因此从未暴露）。修法：dshRuntime.ts 类型静态导入、运行时动态 `import()`，`TransportClosedError` 判定兼容 instanceof + name 回退。typecheck/测试/dev 启动均验证通过。
2. **内置 Node 版本硬伤**：dsh bin 入口用 `import.meta.main` 判断（Node 24 起才支持），内置 Node 22.14 下 CLI 静默 exit 0 什么都不执行——打包后 Agent 必然不工作。修法：`scripts/fetch-node.mjs` 升到 Node 24.19.0，已重新下载（88.5MB）并用 `dsh web` 实测起服成功。

### Agent 升级为完整 dsh Web GUI（进行中）
- 用户反馈：简单聊天 UI 太简陋，没有插件/工作模式。决策：Agent 页直接嵌入官方 `dsh web` 完整 GUI（webview），而非在自有聊天 UI 里重建功能。
- 探测结论：`dsh web --no-open --port 0` 输出带 token 的 127.0.0.1 URL；响应无 X-Frame-Options，可嵌入；webview cookie/auth 用 SameSite=Strict，需要 Electron `<webview>` 标签（独立 guest 进程）。
- 已完成：`src/main/dshWeb.ts`（DshWebGui：启停/URL 截取/30s 超时报错）、`src/main/dshWebProfile.ts`（web profile 物化 + 业务插件同步 + 只挂载不禁用的 patch）、index.ts 三个 IPC（dsh-web-start/stop/status）+ AI 配置保存/工作区迁移时联动重启 + before-quit 清理 + 主窗口 `webviewTag: true`、preload/vite-env 三处同步。
- **进行中**：AiChat.tsx 渲染端 Agent 页改成 webview 嵌入；之后跑 typecheck/测试/真实 dsh web 冒烟。

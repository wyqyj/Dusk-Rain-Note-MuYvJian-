# 工作日志 2026-09-12（收尾提交与文档整理）

## 今日完成

### 1. 质量基线复核
- `npm run build:renderer` 通过（产物同步更新）。
- `npm test`：13 个测试文件、67 用例通过 / 2 跳过（live 门控）。
- `npm run typecheck`：渲染端 + 主进程双侧通过。
- 敏感信息扫描：源码与脚本无 Key 泄漏（凭证仅在 `%APPDATA%\muyujian\dsh-home\.credentials.yaml`，不入仓库）。

### 2. 临时脚本清理
- 删除 `scripts/` 下 14 个一次性脚本：`update-*.mjs` ×10、`fix-dshweb-syntax.mjs`、`ai-chat-debug.mjs`、`ai-chat-smoke.mjs`、`main-fetch-smoke.mjs`。
- 删除 `scripts/app-stdout.log`。
- 保留：`vendor-dsh.mjs`、`fetch-node.mjs`（构建工具），`cdp-eval.mjs`、`cdp-console.mjs`、`shot.ps1`、`click.ps1`、`pick-dialog.ps1`（UI 验证工具），两张验证截图（交接文件引用的证据）。

### 3. 提交当前进度
- 代码提交 `f72ec31`：60 文件，+16478/-884。包含 FIX_PLAN 15 项修复、Agent Bridge + MCP 适配器、内置 dsh Agent（运行时/工具插件/确认闸门/依赖内置）、dsh Web GUI 一级导航与共用配置、AI 知识库（FlexSearch）、Agent 预设框架。
- 仓库级 git 身份沿用历史提交作者 `wyqyj <1484227179@qq.com>`（仅本仓库，未动全局配置）。

### 4. 文档更新
- README：新增「AI 助手」「内置 Agent」「对外接入 Agent」功能章节，技术栈补 FlexSearch。
- HANDOVER：刷新当前状态、建议执行顺序（F2b → F3 → 热重载 → 发布前关卡）、遗留清单。
- KNOWLEDGE_PRESETS_PLAN：阶段 0/1/2 标记完成，FlexSearch 行补「实际使用方式」。

## 下一步
（F2b、F3 与配置热重载已于同日完成，见下方追加；其余保持不变）

1. F3 文档生成插件：`plugins/muyujian-doc-tools/` 三工具（docx/xlsx/pdf）+ 确认闸门 + 真实生成验证。
（F3 已完成，见下方追加）

（配置热重载已完成，见下方追加）

1. 发布前关卡：electron-builder 打包验证 + 全量手工回归 + `DSH_LIVE_KEY` live 测试。

---

## 追加：F2b 研究预设建库与检索（同日推进）

### 1. 架构决策
- 研究库不走独立插件路线，改走统一能力路由：`research.*` 五能力注册进 `src/main/capabilities.ts` 共享注册表，自动透出为 dsh 工具（research_scan 等）与对外 Bridge 能力，与「全部能力沉淀进注册表」原则一致。

### 2. 实现
- 新增 `src/main/researchService.ts`：
  - scan：递归扫描工作区（排除 node_modules/dist/resources/dsh-home/research-db 等，深度 10、单文件 50MB 上限），解析 PDF/DOCX/XLSX/MD/TXT，全文写入 `research-db/texts/`，元数据写入 `research-db/index.json`（mtime+size 增量，删除文件自动剪枝）。
  - list / read / summary / search（FlexSearch，tokenize: full，与知识库同引擎；摘要默认元数据模式：标题取首个 Markdown 标题或首行，outline 取 Markdown 标题，keywords 留空）。
- `src/main/capabilities.ts`：注册 research.scan / research.list / research.read / research.search / research.summary；research.scan 为 write（走确认闸门），其余 read 直接放行。
- `src/main/dshPreset.ts`：研究预设提示词更新为先 research_scan 建 research-db 索引、再 research_search / research_read 检索精读并标注出处。
- 依赖：pdf-parse 2.4.5（类 API：`new PDFParse({ data })` + getText；v2 与 1.x API 完全不同，@types/pdf-parse 已卸载）、mammoth、xlsx。
- 新增 `src/main/mammoth.d.ts` 模块声明（mammoth 无自带类型）。

### 3. 验证
- `npm run typecheck`：渲染端 + 主进程双侧通过。
- `npm test`：75 用例通过 / 2 跳过（新增 researchService 8 例：标题/大纲提取、扫描入库、排除目录、增量复用、变更重解析与剪枝、xlsx 解析、检索）。
- `npm run build:renderer` 通过。
- 真实 PDF 冒烟：手工构造最小合法 PDF + 临时工作区，scan → 解析（type=pdf、标题提取正确）→ search 命中 → read 全文，全链路 OK（临时冒烟脚本已删）。
- 注意：agentBridge 测试在本机代理（127.0.0.1:7897）拦截回环请求时可能偶发失败（fetch 被代理转走），单跑即过，属环境问题，与本次改动无关。

---

## 追加：F3 文档生成（同日推进）

### 1. 架构决策
- 文档生成同样不走独立插件路线，走统一能力路由：doc.createWord / doc.createSheet / doc.createPdf 注册进 `src/main/capabilities.ts`，自动透出为 dsh 工具与对外 Bridge 能力；sideEffect=write，内置 Agent 通道自动走确认闸门。

### 2. 实现
- 新增 `src/main/docService.ts`：
  - createWord：markdown-it 渲染 → html-to-docx 生成 .docx（既有依赖，未新装 docx/exceljs）。
  - createSheet：SheetJS aoa_to_sheet + write 生成 .xlsx（复用 F2b 已装的 xlsx 库）。
  - createPdf：markdown-it → 带中文字体样式的 HTML → Electron 隐藏窗口 printToPDF（运行时动态 import electron，非 Electron 环境给出明确错误）。
- `src/main/capabilities.ts`：注册三个 doc.* 能力，输出统一写入工作台 `documents/` 目录（safeName 消毒文件名，内容 512KB 上限）。
- 复用说明：计划书原方案为 docx（npm）+ exceljs，实际复用既有 html-to-docx 与 xlsx（SheetJS），零新增依赖。

### 3. 验证
- `npm run typecheck`：渲染端 + 主进程双侧通过。
- `npm test`：80 用例通过 / 2 跳过（新增 docService 5 例：markdown/HTML 渲染、docx 经 mammoth 回读验证、xlsx 经 SheetJS 回读验证、非 Electron 环境 PDF 明确拒绝）。
- 真实 Electron PDF 冒烟：临时 Electron 主进程脚本调用 DocService.createPdf，生成 21404 字节合法 PDF（magic %PDF-，中文 + 加粗 + 列表），临时冒烟脚本已删。

---

## 追加：dsh 配置热重载（同日推进）

### 1. 实现
- 新增 `src/main/dshConfigWatcher.ts`：`fs.watch` 监听 dsh home 目录（settings.yaml / .credentials.yaml），900ms 防抖 + `fingerprintConfig` 指纹比对（baseUrl/model/providerId/apiKey），只在有效配置实际变化时触发，自身播种或无关文件不触发。
- `src/main/index.ts`：dshWebGui 创建后启动监听；onChange 时 `dshRuntime.restart()`（惰性丢弃，安全）+ `dshWebGui.isRunning()` 时才 `dshWebGui.restart()`（避免后台拉起用户没打开的 web 子进程）；before-quit 停止监听。

### 2. 验证
- `npm run typecheck` 双侧通过。
- `npm test`：83 用例通过 / 2 跳过（新增 dshConfigWatcher 3 例：指纹唯一性、写入 settings.yaml 触发 onChange、无关文件不触发）。

---

## 追加：研究预设插件加载修复 + 知识库打包 UI 美化与 Agent 联动（用户反馈修复）

### 1. 修复：切换研究模式报 Cannot find module '@deepseek-ai/dsh-tools'
- 根因：`.agent-presets\<id>\agent.cordis.yml` 引用 `./tools/index.js`，但 syncNativeAgentPresets 只拷贝 index.js/package.json，没有 node_modules；插件 `require('@deepseek-ai/dsh-tools')` 无法解析（dsh-tools 依赖为 peer + schemastery，整包拷贝太脆）。
- 修法（回退解析，不整包拷贝）：
  - `src/main/dshRuntime.ts` 新增 `resolveDshToolsDir(dshBin)`（dsh 包的同级依赖目录），launch env 注入 `MUYUJIAN_DSH_TOOLS_PATH`；`dshWebProfile.ts` 转发导出，`dshWeb.ts` launch 同样注入。
  - `plugins/muyujian-dsh-tools/index.js`：先 bare require，失败时回退到 env 注入的绝对路径 `require(<pkg>/lib/index.js)`（标准 profile 路径行为不变）。
- 验证：node 冒烟确认 resolveDshToolsDir 解析到真实存在的 dsh-tools 目录；85 用例 + typecheck + build:renderer 全绿。重启应用生效（syncNativeAgentPresets 每次 web launch 重新拷贝插件）。
- 隔离模拟：把修复后的 index.js 拷到无 node_modules 的临时目录 require + apply，回退解析成功、apply 正常完成；对磁盘上旧拷贝 require 则精确复现用户报错（旧拷贝在应用重启前未更新）。

### 2. 知识库打包 UI 美化（AiChat）
- bundle 面板 JSX 重写：内联样式改为语义化 class；新增选中态卡片（左侧高亮条 + 描边）、来源计数徽章、描述行与 hover 态。
- 新增「已选 N 个知识库包」清空栏；空状态改为居中提示卡片。
- CSS：`ai-bundle-*` 全套重写（浅色 + `.dark` 暗色适配）。

### 3. 打包知识库与 Agent 联动
- capabilities 新增 `knowledge.bundles`（列出打包知识库，自动透出 dsh 工具与对外 Bridge）。
- `knowledge.search` schema 新增 `bundleId`（单个包过滤，dsh 工具可用的标量参数）；handler 兼容 bundleId / bundleIds。
- 语义修正：显式传包过滤但解析不到来源时返回空（此前会回退检索全部）。
- 暮雨笺预设提示词：先 knowledge_bundles 列包，再用 knowledge_search 的 bundleId 在包内检索。
- 单测：`src/main/capabilities.test.ts` 2 例（knowledge.bundles 列包、bundleId 过滤生效与无效包返回空）。

---

## 追加：完整功能检查（同日收尾）+ 预设切换 IPC 缺失修复

### 1. 完整检查结果
- 质量基线：build:main / typecheck / `npm test` 85 用例 + 2 跳过 / build:renderer 全绿。
- 真实 dsh 集成测试（`DSH_INTEGRATION=1`）：1 例通过。
- 真实网关 live E2E（凭据注入 Key，不打印）：中文路径工作区 + kimi-k3 调用 notes_create 建笔记，1 例通过（105s）。
- 真实应用 CDP 自动化：
  - AI 助手：页面渲染、配置就绪、发送「只回复一个词:好的」→ 网关回复「好的」→ streaming 正常结束、光标消失（期间网关极慢约 90s，为外部因素；node 直连诊断同样 90s+）。
  - Agent：webview 加载、`dshWebStatus().running=true`、无错误态。
  - 研究预设：boot 与切换均成功，webview 拿到新端口，无 Cannot find module 报错；截图确认 Web GUI 完整渲染（会话历史、kimi-k3、研究模式下拉可见）。
  - 能力清单：muyujian-tools.json 21 个能力全部注册（notes/plan/questionBook/knowledge 含 bundles/research 五工具/doc 三工具）。

### 2. 新发现并修复：dsh-web-restart IPC 缺失（AgentView 预设切换整个失效）
- 根因：preload 暴露 `dshWebRestart`（invoke `dsh-web-restart`），但 index.ts 从未注册该 handler——AgentView 点暮雨笺预设/研究预设切换必然报 No handler registered；且 `dsh-web-start` 忽略 preset 参数，boot 时 preset 被丢弃（localStorage 是 research 也只会用默认预设启动）。
- 修法（src/main/index.ts）：
  - 新增 `dsh-web-restart` handler：校验 preset（muyujian/research）后调 `dshWebGui.restart(target)`。
  - `dsh-web-start` 接收 preset 参数并传给 `ensureUrl(target)`。
- 验证：重启应用后 CDP 实测——boot 以研究预设启动成功（webview 新端口 61340、无错误）；点暮雨笺预设切换成功（新端口 52701、activePreset 正确、webRunning=true），dsh-web-restart IPC 真实生效。

### 3. 说明
- 用户最初报的「failed to apply loader entry」来自 dsh Web GUI 内部预设下拉（.agent-presets 加载路径），由 MUYUJIAN_DSH_TOOLS_PATH 回退解析修复；AgentView 分段切换控件的失效由本次 IPC 修复。两条路径现在都验证通过。

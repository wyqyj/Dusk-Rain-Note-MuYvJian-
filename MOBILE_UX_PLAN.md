# 暮雨笺 Android 移动端优化方案（基于用户反馈 6 条，已实施）

> **状态：✅ 已全部实施**（2026-08-18）——详见各节与 `VERIFY.md` 验证清单。数据目录迁移按确认结果**仅移动端移除**。

> 原则：所有改动**平台感知**（`platform-capacitor` / `Capacitor.isNativePlatform()`），
> 桌面（Electron/浏览器宽屏）行为不变；数据格式与备份协议不动，桌面↔手机迁移不受影响。

---

## 1. 编辑器单栏：笔记与预览不同屏，可开关

**现状**：`App.tsx` L341 并排渲染 `<Editor /> + <Preview />`（宽屏 50/50）；顶栏已有预览开关按钮（L251），`AppearancePanel` 也有「实时预览」开关。移动端窄屏并排挤压。

**方案（移动端）**：编辑区改为**单栏两态切换**——一次只显示一个：
- `showPreview` 初始 `false`（移动端）；点顶栏预览按钮切换到全宽 Preview，再点切回 Editor。
- 渲染分支（`App.tsx` L341）平台化：
  ```
  移动端: showPreview && !focusMode ? <Preview/> : <Editor/>
  桌面:   保持现状（并排）
  ```
- 开关入口沿用现有顶栏按钮与设置项，无需新增 UI。

**涉及文件**：`src/ui/App.tsx`（初始状态 + 渲染分支）
**验收**：手机上编辑时只见编辑器；点预览按钮全屏预览；再点回编辑。桌面并排不变。

---

## 2. 画布功能移动端剔除

**现状**：入口有 3 处 —— Sidebar「新建画布」按钮（L250）、Sidebar 左侧「画布」分类（L267）、命令面板「新建画布」（`App.tsx` L374）。点击画布便签进入 `CanvasBoard`（L341 isCanvas 分支）。

**方案（移动端）**：
- 隐藏上述 3 处新建/分类入口（平台条件渲染）。
- 既有画布便签（桌面迁移数据）点击后：显示**占位提示卡**（"画布功能仅在桌面版支持，请用电脑端编辑此便签"；若 `canvasOutline` 存在则顺带渲染只读大纲列表方便导读），不再进入 CanvasBoard。
- 桌面入口与画布编辑完全不变。

**涉及文件**：`Sidebar.tsx`、`App.tsx`、`CommandPalette` 调用处（新增画布项）
**验收**：手机上无任何画布入口；点开旧画布便签见提示卡；桌面画布一切照旧。

---

## 3. 底部 UI 排版优化

**现状盘点**：
- App 底部状态栏 `app-statusbar`：上游已在小屏隐藏快捷键（`index.css` L485）；`platform-capacitor` 全屏范围兜底隐藏。
- 工作台底部导航 `study-mobile-nav`（`StudyWorkbench.tsx` L448）：**8 列**按钮 总览/规划/专注/书架/题册/笔记/**画布**/设置，一行挤 8 个，且「画布」项应随第 2 条移除。

**方案（移动端）**：
- `nav` 数组移除「画布」项 → 剩 7 项；CSS `study-mobile-nav` 网格由 `repeat(8,1fr)` 改为自适应列数（`repeat(auto-fit, minmax(48px,1fr))` 或显式 7 列）。
- 底部按钮保持 44px+ 触控高度（上游已设），字体 10px→11px 微调提升可读性。
- 状态栏在平台级兜底隐藏（防平板横屏 >767px 时复活）。

**涉及文件**：`StudyWorkbench.tsx`（nav）+ `styles/index.css`（study-mobile-nav 网格、状态栏平台隐藏）
**验收**：手机工作台底部一排清爽 7 项、可点间隙大；无画布项；状态栏不出现快捷键提示。

---

## 4. 导出备份 / 导入恢复 → 设置面板

**现状**：入口在工作台页头 `study-header-actions`（`StudyWorkbench.tsx` L424「导出综合备份/导入恢复」），日常在笔记侧找不到。

**方案**：
- `AppearancePanel`（设置面板）新增「数据备份与恢复」区：**导出综合备份**、**导入恢复**两按钮（调 `getBridge().workspace.backupWorkspace()/restoreWorkspace()`，成功/取消给出提示）。两平台统一可用（桌面也顺手）。
- 移动端工作台页头两按钮**隐藏**（防重复且工作台入口对笔记用户不直观）；桌面保留工作台入口。
- 恢复流程（系统文件选择器 → SHA-256 校验 → 覆盖）不变。

**涉及文件**：`AppearancePanel.tsx`（新增区块）、`StudyWorkbench.tsx`（平台条件隐藏页头按钮）
**验收**：设置面板可完成导出/导入；工作台页头移动端无重复按钮；桌面两处并存可用。

---

## 5. Agent 提示词：一键复制，不展示全文

**现状**：题册技能区与 `PromptResourcePanel` 都渲染 `<textarea class="agent-prompt">` 显示完整提示词 + 一个复制按钮；移动端大段文本占屏。

**方案（移动端）**：
- 隐藏 `agent-prompt` 文本框（CSS 平台规则），保留「复制 Agent 提示词」按钮与技能/提示词路径信息。
- 复制走 `navigator.clipboard`（capacitor https scheme 属 secure context，可用）；失败 fallback 文案去掉"下方文本框中手动复制"字眼（文本已隐藏），改为提示。
- 桌面保持文本区展示。

**涉及文件**：`styles/index.css`（平台隐藏）+ `StudyWorkbench.tsx`（fallback 文案微调）
**验收**：手机上一键复制成功、页面不显示提示词全文；桌面照旧。

---

## 6. 数据目录迁移移除

**现状**：工作台设置页「数据目录与综合备份」区含 `WorkspaceRoot`（"迁移数据目录"）+ SHA-256 迁移说明；移动端本就固定沙箱目录，迁移无意义。

**方案**：
- **移动端**：移除该区（连同备份按钮一并腾位，备份已迁到设置）。
- **桌面**：默认保留（等确认后决定是否全局移除——见下方待确认点）。

**涉及文件**：`StudyWorkbench.tsx`（平台条件渲染）
**验收**：手机工作台设置页无数据目录/迁移相关内容。

---

## 实施记录

- 每步后跑 `tsc --noEmit`（0 错误）+ `vitest`（8 文件 24 用例）+ `vite build` 均通过。
- 最终 APK：`dist-apk/暮雨笺-3.0.6-debug.apk`（移动端全局适配）。

## 待确认点（影响实施范围）

1. **数据目录迁移**："不再需要"是仅移动端，还是全局（桌面也要删）？本方案默认仅移动端移除。
2. **底部导航形态**：移除画布后剩 7 项是否即"排版优化"的预期？如需更精简（如 5 项 + 更多菜单），请说明期望保留的核心项。

## 实施与回归

按 1→6 顺序实施，每步后跑 `tsc --noEmit` + `vitest run` + `vite build`；全部完成后 `cap sync` + 重建 APK，并更新 `VERIFY.md` 验证清单。
# 暮雨笺功能优化计划：计划提示词、题册 PDF、画布滚轮

本计划针对新增需求，沿用当前工作台的文件资源、主进程 IPC 和设置面板模式。当前阶段只整理方案，不修改业务实现。

## 一、目标与预期效果

1. 计划文件导入拥有与题册整理一致的 Agent 提示词，用户可以在设置中查看、复制和打开提示词资源。
2. 题册支持按题目数量导出 PDF，导出的题目范围、顺序和筛选条件明确，避免手工复制题目。
3. 画布模块内部滚轮只滚动模块内容，不再触发整张画布缩放；在画布空白区域滚轮仍保持缩放能力。
4. 相关解析、题目选择和滚轮边界具备纯函数测试，后续扩展不会依赖巨型组件中的隐式状态。

## 二、Phase 1：计划导入提示词资源化

### 2.1 提示词内容

新增“计划 Markdown 整理”提示词，要求 Agent：

- 从用户提供的计划、截图 OCR 结果或文字中提取可执行任务。
- 每项任务单独占一行，只使用 `- [ ] 任务名称` 格式。
- 删除说明文字、编号、表格和空任务；一个任务中保留必要的时间、科目或章节信息。
- 已完成事项默认不输出；如果用户明确要求保留完成记录，使用 `- [x] 任务名称`。
- 不擅自拆分用户没有要求拆分的任务，不改变原意，不补造截止日期。
- 最终只输出可直接保存为 `.md` 的任务清单，并给出最小示例。

提示词的输出契约要与导入解析器一致：至少保证 `- [ ] 任务名称` 可被导入；后续可扩展 `[x]`、科目和截止日期字段，但不能破坏基础格式。

### 2.2 资源和设置入口

新增资源目录：

```text
skills/muyujian-plan-import/
├── SKILL.md
└── references/
    └── agent-prompts.md
```

实现步骤：

1. 将资源加入 `package.json` 的 `extraResources`，开发环境和打包环境使用同一定位逻辑。
2. 主进程新增 `workspace-plan-skill` IPC，返回资源目录、技能文件、提示词文件和提示词内容。
3. preload 与 `vite-env.d.ts` 增加类型化 API。
4. 设置页新增“计划 Markdown 整理提示词”面板，支持加载、复制、打开资源目录和打开提示词文件，并显示资源读取失败状态。
5. 计划导入按钮旁增加“查看整理提示词”的入口，跳转到设置页，减少用户寻找成本。

### 2.3 计划导入兼容性

- 抽取 `parseMarkdownTasks` 纯函数，继续支持 `- [ ] 任务名称`。
- 同时识别 `- [x]`，将 `completed` 映射为 `true`，避免提示词保留完成记录时被静默丢弃。
- 忽略缩进、允许 `*`/`+` 列表标记，清理任务名称两端空白。
- 导入前展示任务数量和完成数量；空文件、格式错误和重复导入给出明确提示。
- 本轮不自动猜测科目和截止日期；默认科目保持当前逻辑，避免错误推断。

## 三、Phase 2：题册按数量导出 PDF

### 3.1 导出范围定义

题册页面增加导出控件，默认导出当前题册中“当前筛选结果”的前 N 题：

- 范围：全部题目、当前章节或当前筛选结果。
- 数量：数字输入，最小 1，最大为当前范围题目数；提供“全部”快捷选项。
- 顺序：沿用题册中的章节和题号顺序，不按随机顺序导出。
- 内容：题号、章节、状态、题干、标准答案、我的答案、错误原因和标签；空字段不生成空标题。
- 文件名：`题册名-范围-N题.pdf`，并清理 Windows 不允许的文件名字符。

### 3.2 实现方式

1. 新增 `questionBookExport.ts` 纯函数，将 `Question[]` 渲染为 Markdown/LaTeX 兼容文本。
2. 在题册页增加范围选择、数量输入、导出按钮、导出中状态和失败提示。
3. 复用现有 `export-pdf` IPC，继续使用主进程已有的 Pandoc/XeLaTeX 导出链，不重复实现文件保存对话框。
4. 导出前固定快照，避免用户在异步导出期间修改题册导致 PDF 内容与提示数量不一致。
5. 对 Pandoc/LaTeX 不可用的情况返回明确错误，提示用户安装或配置依赖；不生成伪成功文件。

### 3.3 验收标准

- 输入 10 时只导出 10 题；输入超过范围、空值或非数字时不能发起导出。
- “当前章节”和“当前筛选”组合时，数量上限与实际候选题数一致。
- 导出的 Markdown 中公式仍能被 Pandoc 正确处理，题目顺序和中文内容完整。
- 取消保存对话框不显示错误，不修改题册数据。

## 四、Phase 3：画布模块滚轮与画布缩放隔离

### 4.1 交互规则

- 鼠标位于画布空白区域：滚轮继续缩放画布，并以鼠标位置为缩放中心。
- 鼠标位于文本题目模块、关联笔记模块或模块内编辑器：滚轮只滚动该模块的内容，事件不再冒泡到画布视口。
- 模块没有可滚动内容时，滚轮也不能意外改变整张画布的缩放比例。
- 触控板/滚轮的 `ctrl`/`meta` 缩放行为保持一致，但只在画布空白区域生效。

### 4.2 实现边界

1. 在 CanvasBoard 中抽取 `isCanvasBackgroundWheelTarget` 或等价的目标判断函数。
2. 视口 `onWheel` 先判断事件是否来自 `[data-canvas-item]`；模块内事件直接停止冒泡并交给原生滚动。
3. 为文本预览、题目模块和编辑器容器设置明确的 `overflow-y: auto`、最大高度和 `overscroll-behavior: contain`，避免滚动链传递到视口。
4. 保持拖拽、缩放手柄、右键菜单和模块外缩放逻辑不变。
5. 滚轮处理函数不直接修改元素坐标或画布内容，只改变相应的滚动容器或 camera 状态。

## 五、Phase 4：测试与回归

### 单元测试

- `parseMarkdownTasks`：空任务、缩进、`[ ]`、`[x]`、`*`/`+` 和非法行。
- `renderQuestionBookMarkdown`：数量截取、章节筛选、字段缺省、文件名清理。
- 滚轮目标判断：模块内不缩放画布，背景区域正常缩放。

### 手工回归

- 设置页能加载、复制和打开计划提示词；开发和打包路径均可用。
- 使用提示词生成的 Markdown 可直接导入，并正确显示未完成/已完成数量。
- 题册按 1、N、全部三种数量导出，取消和 Pandoc 失败路径均有反馈。
- 题目模块内部滚动不会改变右上角缩放百分比；空白区域仍可缩放。

### 验收命令

```bash
npm run typecheck
npm test
npm run build
```

## 六、实施顺序与收益

建议顺序为：先抽取计划解析和题册导出纯函数，再接入 IPC/设置 UI，最后修改画布事件边界；每一步都可以独立测试。

完成后，Agent 生成的计划文件将有稳定格式和可发现的操作入口，题册复习资料可按需生成小范围 PDF，画布滚动与缩放不会互相干扰。整体上会减少手工整理、降低误操作，并为后续批量导出、更多计划字段和更复杂画布模块保留扩展点。
